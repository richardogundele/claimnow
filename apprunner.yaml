version: 1.0
runtime: python311
build:
  commands:
    build:
      - pip install -r requirements.txt
run:
  command: uvicorn src.main:app --host 0.0.0.0 --port 8000
  network:
    port: 8000
    env: HTTP
