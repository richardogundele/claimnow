# =============================================================================
# ClaimsNOW v2 - Local ML Dependencies
# =============================================================================
# No cloud services - everything runs locally
# =============================================================================

# -----------------------------------------------------------------------------
# LangChain - LLM Application Framework
# -----------------------------------------------------------------------------
# LangChain provides the structure for building LLM apps:
# - Chains: sequence of calls (parse -> extract -> score)
# - Agents: LLM decides which tools to use
# - Memory: conversation history
# - Retrievers: interface to vector stores
langchain>=0.1.0
langchain-community>=0.0.20

# -----------------------------------------------------------------------------
# Vector Database - ChromaDB
# -----------------------------------------------------------------------------
# ChromaDB stores embeddings and enables similarity search
# Used for RAG: find similar market rates to compare against
chromadb>=0.4.22

# -----------------------------------------------------------------------------
# Embeddings - Sentence Transformers
# -----------------------------------------------------------------------------
# Creates vector embeddings from text locally
# No OpenAI API needed - runs entirely on your machine
sentence-transformers>=2.3.0

# -----------------------------------------------------------------------------
# Document Parsing - PDF Text Extraction
# -----------------------------------------------------------------------------
# PyMuPDF (fitz): Fast PDF text extraction
# pdfplumber: Better table extraction
# Both run locally, no AWS Textract needed
pymupdf>=1.23.0
pdfplumber>=0.10.0

# -----------------------------------------------------------------------------
# OCR - Optical Character Recognition (optional)
# -----------------------------------------------------------------------------
# For scanned PDFs that don't have text layer
# Requires Tesseract installed on system
pytesseract>=0.3.10
Pillow>=10.0.0

# -----------------------------------------------------------------------------
# Machine Learning - scikit-learn
# -----------------------------------------------------------------------------
# Classic ML algorithms: classifiers, preprocessing, metrics
# Used for: claim scoring model, feature engineering
scikit-learn>=1.4.0

# -----------------------------------------------------------------------------
# Deep Learning - PyTorch
# -----------------------------------------------------------------------------
# Neural network framework
# Used for: custom models, fine-tuning (if needed)
torch>=2.1.0

# -----------------------------------------------------------------------------
# Explainability - SHAP
# -----------------------------------------------------------------------------
# SHapley Additive exPlanations
# Shows WHY the model made each prediction
# Critical for insurance - decisions must be explainable
shap>=0.44.0

# -----------------------------------------------------------------------------
# Data Processing
# -----------------------------------------------------------------------------
# pandas: DataFrames for structured data
# numpy: Numerical arrays
pandas>=2.1.0
numpy>=1.26.0

# -----------------------------------------------------------------------------
# API Framework - FastAPI
# -----------------------------------------------------------------------------
# Modern async web framework for Python
# Automatic OpenAPI documentation
fastapi>=0.109.0

# uvicorn: ASGI server to run FastAPI
uvicorn>=0.27.0

# python-multipart: Required for file uploads
python-multipart>=0.0.6

# -----------------------------------------------------------------------------
# Configuration & Utilities
# -----------------------------------------------------------------------------
# pydantic: Data validation
# python-dotenv: Load .env files
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# Date parsing
python-dateutil>=2.8.2

# -----------------------------------------------------------------------------
# HTTP Client
# -----------------------------------------------------------------------------
# For calling Ollama API
httpx>=0.26.0
requests>=2.31.0

# -----------------------------------------------------------------------------
# Testing
# -----------------------------------------------------------------------------
pytest>=7.4.0
pytest-asyncio>=0.23.0
