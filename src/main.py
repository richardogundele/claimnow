# FastAPI application entry point
# Defines REST API endpoints:
# - POST /analyse: Accept PDF upload, return claim analysis
# - GET /health: Health check endpoint
# - GET /claims/{id}: Retrieve previous analysis results
